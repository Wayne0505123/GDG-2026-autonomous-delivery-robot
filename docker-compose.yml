version: '3.8'

services:
  # 1. 資料庫服務
  db:
    image: postgres:15-alpine
    container_name: robot-db
    restart: always
    environment:
      - POSTGRES_USER=robot_user
      - POSTGRES_PASSWORD=robot_pass
      - POSTGRES_DB=robot_db
    volumes:
      - postgres_data:/var/lib/postgresql/data  # <--- 關鍵 1：掛載路徑
    ports:
      - "5433:5432"  # <--- 我幫你打開了，這讓你可以用本機連進去測試 (Host Port: 5433)

  # 2. 後端服務
  backend:
    build: .
    container_name: robot-backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    depends_on:
<<<<<<< HEAD
      - db
=======
      - db # 確保資料庫先啟動
    env_file:
      - .env
>>>>>>> 0d44fe25c659fffe92c63538daf3db0a381cefe3
    environment:
      - DATABASE_URL=postgresql://robot_user:robot_pass@db:5432/robot_db

  # 3. 前端服務
  frontend:
    build: ./frontend
    container_name: robot-frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://localhost:8000
      - VITE_API_TARGET=http://backend:8000

# 4. 定義磁碟區
volumes:
  postgres_data:  